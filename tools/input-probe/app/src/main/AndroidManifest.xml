<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:label="Input Probe"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".ResearchNotificationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="org.sableos.research.inputprobe.POST_TEST_NOTIFICATION" />
                <action android:name="org.sableos.research.inputprobe.CANCEL_TEST_NOTIFICATION" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
